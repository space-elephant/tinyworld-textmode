#!python3                               from collections import namedtuple      point = namedtuple("point", ["x", "y"]) up = point(0, 1)                        down = point(0, -1)                     left = point(-1, 0)                     right = point(1, 0)                     def forward(pos, dir):                   return point(pos.x + dir.x, pos.y + dirdef leftf(dir):                          if dir == up:                            return left                            elif dir == down:                        return right                           elif dir == left:                        return down                            elif dir == right:                       return up                              else:                                    raise Exception("Ant failed!")        def rightf(dir):                         if dir == up:                            return right                           elif dir == down:                        return left                           N